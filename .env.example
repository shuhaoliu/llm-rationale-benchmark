# Environment variables for rationale benchmark
# Copy this file to `.env` and replace placeholder values with your real secrets.
# Keep `.env` out of version control (already handled by `.gitignore`).

# OpenAI models (e.g., gpt-4). Key usually starts with "sk-".
OPENAI_API_KEY="sk-your-openai-api-key"

# Anthropic Claude models. Key typically begins with "sk-ant-".
ANTHROPIC_API_KEY="sk-your-anthropic-api-key"

# Google Gemini models. Use the Generative Language API key that starts with "AIza".
GEMINI_API_KEY="AIza-your-gemini-api-key"

# Optional: OpenRouter aggregator support from example configs.
OPENROUTER_API_KEY="sk-or-your-openrouter-api-key"

# Add any additional provider secrets following the PATTERN_NAME convention.
# For example: PROVIDER_X_API_KEY="your-provider-x-api-key"
